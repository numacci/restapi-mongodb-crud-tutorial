# Web
server:
  servlet:
    context-path: /api
# MongoDB
spring:
  data:
    mongodb:
      host: localhost
      port: 27017
      username: appuser
      password: appuser
      authentication-database: appdb
      database: appdb
